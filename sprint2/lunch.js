const meals = ['subway', 'sushi', 'kebab']

function lunch (meals){
    if (meals[0] === 'subway'){
        console.log("eat fresh")
    } else {
        console.log("don't eat fresh")
    }
}

lunch(meals[2])